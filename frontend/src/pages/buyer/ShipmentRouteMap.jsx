// Re-exports the component from the shared buyer component location.
// Both pages/ and components/ directories had a copy — now they share one source.
export { default } from '../../components/buyer/ShipmentRouteMap';